export const styles = {
  container: {
    flex: 1, // Faz com que o contêiner ocupe todo o espaço disponível
    justifyContent: "center", // Centraliza o conteúdo verticalmente (opcional)
    alignItems: "center", // Centraliza o conteúdo horizontalmente (opcional)
  },
};
